spring:
  jpa:
    open-in-view: true
    show-sql: true

  output:
    ansi:
      enabled: always
  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:user
    username: sa
    password:

  profiles:
    include: oauth

